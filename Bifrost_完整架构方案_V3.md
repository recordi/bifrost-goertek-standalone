# Bifrost — 数据分析与可视化 AI 超级工作台

> **完整架构方案 V3** · 以歌尔智能硬件交付场景为首个落地对象 · 2026-08-04
>
> 相对 V2 的变化：**推翻了 V2 自己提出的"规格驱动渲染"路线**，改为模型自由手写产物 + 事实集绑定；洞察层拆为三段并新增事实评审点；Agent 收敛为六个模型节点与两个评审点；新增 Agent 记忆分层与分环境安全策略；对 open-design 自检机制做了源码级核实并更正了 V2 的描述。

## 一、定位：我们要造的是什么

Bifrost 是一个数据分析与可视化的 AI 超级工作台。用户接入企业数据、用自然语言提出诉求，系统自动完成数据体检与治理建议、指标口径对齐、多维分析、看板生成、可信度校验与岗位化交付。它对标 open-design 的形态与工程品味，但要解决的是完全不同的问题：open-design 追求"好看且可信的设计稿"，Bifrost 追求"好看且**数字是真的**的决策物"。

判断这个项目成不成的标准只有一条：**能不能让 agent 自主产出媲美顶级数据工程师的看板，并且让人敢照着它做决定。**前半句关乎审美与洞察，后半句关乎可信。V2 为了后半句差点牺牲了前半句，V3 把这个错误纠回来了——这是本版最重要的一处改动，详见 2.2。

### 1.1 与 open-design 的三个根本差异

**一、数字必须是真的。**open-design 的看板 skill 明确允许模型编造"看起来合理"的数值，因为它交付的是设计稿。我们交付的是决策依据，所以数值必须可追溯到具体的数据集、过滤条件与聚合路径。这条差异派生出了 Bifrost 一半以上的架构。

**二、必须有口径唯一的语义层。**同一个"良率"在不同部门有不同算法，这是所有企业数据项目的第一号杀手。open-design 完全没有这一层，因为设计稿不需要。我们把指标定义外置成可评审的 YAML，让口径成为唯一事实源。

**三、必须能看见自己画出来的东西。**这是本轮源码核实后表述更精确的一条：open-design 有两套自检机制，但**两套都是纯文本读源码，仓库里没有任何无头浏览器截图依赖**。溢出、遮挡、重叠、可读性这些只能"看"出来的问题，它结构上就发现不了。详见 2.4。

### 1.2 对标矩阵

| **维度** | **open-design** | **通用大模型直接生成** | **传统 BI（Tableau/FineBI）** | **Bifrost** |
| --- | --- | --- | --- | --- |
| 产物形态 | 自包含单文件 HTML | 一段代码或图 | 拖拽式看板 | 自包含 HTML + 交互版，双形态共用一份事实集 |
| 审美水准 | 高（专家知识外置为文件） | 低且不稳定 | 中，受模板限制 | 高（整体移植其设计知识资产） |
| 数值可信 | 允许编造 | 常编造 | 可信但要人配 | 事实集绑定 + 六道 gate 硬阻断 |
| 指标口径 | 无此概念 | 无 | 需人工建模 | metrics.yml 语义层，唯一事实源 |
| 视觉自检 | 读源码文本，无截图 | 无 | 无 | 截图多模态 + 可路由修正 |
| 岗位适配 | 无 | 需反复提示 | 需分别搭建 | 岗位画像文件驱动，一套链路跑多岗位 |
| 可扩展性 | 技能热加载、影子覆盖 | 无 | 需二次开发 | 沿用其技能体系 + 领域技能库 |

## 二、吸收与改造：明确哪些照学，哪些必须推翻

### 2.1 四条整体吸收的机制

**一、专家知识外置为可评审文件。**这是 open-design 最值钱的东西，也是 Bifrost 的思想内核。它让模型的专业性不靠模型更聪明，而是把顶级设计师的判断写成 DESIGN.md、tokens.css、craft 规则这些人类可读可改可版本管理的文件，在每次生成时按固定顺序注入。移植到数据领域即：把顶级数据工程师的判断写成机器可读的指标口径与人类可读的分析方法 skill。业务专家能直接改文件而不用碰代码，这同时解决了"AI 不懂业务"和"知识沉淀"两个老问题。

**二、不自研 agent loop，只做编排与运行时适配。**它把模型调用与工具使用全部委派给外部 code-agent 执行体，新增一种模型只需在 runtimes 目录加一个描述文件。我们保留这一层，好处是可以自由接入国内模型与私有部署模型；飞书 Aily 也可以挂在这一层作为可替换的执行体与对话入口——但编排逻辑必须留在 Bifrost 自己的服务端，不能下放给 Aily（原因见第五章）。

**三、技能的热加载与影子覆盖。**技能与模板都是 SKILL.md 加扩展 frontmatter，用户目录同名文件可覆盖内置版本，每次请求重新扫描、无需重启。这让"领域知识可运营"成为可能：歌尔的工艺专家可以自己维护一套 OEE 损失树分解规则，覆盖我们的通用版本。这是产品从演示走向可持续的关键。

**四、产物形态与工作台交互。**自包含单文件 HTML、沙箱 iframe 预览、参数面板即时调参（筛选性参数不重跑模型）、Studio 画布手动微调、技能市场一键使用、多格式导出。这些照学，并针对数据场景增强。

### 2.2 一条必须自我推翻的：从"规格驱动渲染"退回"模型手写产物"

**V2 的 L4「规格层」曾被写成架构核心创新，V3 明确撤销这个定位。**V2 的设计是：模型只输出 Dashboard Spec 与 Chart IR 两份 JSON，由我们的渲染器把 JSON 变成 HTML。这个方案在"可校验、可复现、可增量"三项上确实成立，但它有一个致命代价——**看板长什么样，上限被我们渲染器的代码写死了。**模型无法为一次特殊的业务诉求换一种版式、加一段手绘示意、为某条产线做一个别处没有的对比控件。而这恰恰是 open-design 之所以好看的全部原因：它让模型自由地写 HTML 与内联 SVG，约束只来自注入的设计系统与 craft 规则，而不来自渲染器的能力边界。

一个只会渲染我们预想过的图表的系统，永远做不到"媲美顶级数据工程师"。顶级数据工程师的价值恰恰在于遇到没见过的问题时能设计出没见过的表达。

**V3 的做法是把约束从"限制模型能画什么"改成"限制模型能用什么数"。**具体是三条：

- **产物由模型手写。**制作 agent 直接产出完整的自包含 HTML，图表可以是手绘内联 SVG，也可以是 ECharts option，也可以是纯 CSS 的进度条与热力格，由它自己判断哪种表达最合适。我们不规定。
- **数字不由模型写。**所有数值先由确定性代码算成一份**事实集 FactSet**，随产物以数据岛形式注入。模型在 HTML 里写数字的唯一合法方式是引用事实 id：`<span data-fact="oee.a01.w31">72.3%</span>`。凡是没有被 `data-fact` 包裹的数字，一律视为编造，出稿前被拦。
- **预置模板是脚手架，不是牢笼。**我们提供一套看板模板与图表片段库，作为起手式与质量基线，模型可以整段替换、可以只借用其中的排版骨架、也可以完全不用。模板的作用是抬高下限，不是压低上限。

这三条合起来的效果是：**模型的表达自由度回到 open-design 的水平，而数值可信度不降反升。**因为"不许编数字"不再依赖提示词里的一句请求，而是变成了一个结构性事实——模型手上根本没有数字，它只有事实 id。

保留下来的部分是：Flint Chart IR 与 Dashboard Spec 不再是必经通路，降级为两种可选工具——一是模板库里生成标准图表的脚手架，二是误导性编码规则的校验对象之一。渲染器仍然存在，但它服务于"交互版与离线版共用同一份事实集"这个目标，而不是充当模型与产物之间的强制关口。

### 2.3 两条必须推翻 open-design 的默认

**推翻"数值可编造"。**这是 Bifrost 与市面上所有"AI 生成看板"的分水岭，机制即上文的事实集绑定。值得说明的是它的工程代价很低：一个数据岛注入器加一条正则比对的 gate，几百行代码，却把整个产品从演示品变成了生产系统。

**推翻"自检只读源码"。**升级为三级验证：DOM 级结构校验、事实与口径的确定性比对、无头浏览器截图的多模态回看。前两级不消耗模型调用，第三级是 open-design 结构上缺失的那一环。

### 2.4 open-design 自检机制的源码级核实结论

这一节是 V2 的一处事实性更正。V2 写的是"它的自检只有 design-templates/critique 五维评分读源码，加一个 artifact lint 的 pre-emit gate"。逐文件核实（main 分支，package.json 版本实为 0.16.1）后发现，**它其实有两套完全不同的评审机制，而 V2 只提到了较弱的那一套。**

**第一套是 `design-templates/critique/SKILL.md`**，一个出报告的渲染模板。纯手动触发，靠用户 brief 命中 triggers；五个维度是 Philosophy consistency、Visual hierarchy、Detail execution、Functionality、Innovation，各 0–10 分，档位为 0–4 Broken / 5–6 Functional / 7–8 Strong / 9–10 Exceptional；纪律条款写得相当讲究——必须引用证据、取"最差的持续区间"而不许求平均、总均分高于 8 视为可疑。但它**没有任何程序化阈值，不阻断任何东西**，输出一份报告就结束。它甚至明确禁止自评："Don't review your own artifact in the same turn — the user needs to see it first."

**第二套是 `apps/daemon/src/critique/`（内部代号 Critique Theater，产品名 Design Jury）**，这才是真正的评审编排系统，V2 完全漏了。它在 daemon 侧实现了状态机、协议解析、打分与落库：五个角色 DESIGNER / CRITIC / BRAND / A11Y / COPY，其中 DESIGNER 只出稿不打分且不计入综合分，权重为 critic 0.4、brand 0.2、a11y 0.2、copy 0.2；阈值 8.0、满分 10、最大 3 轮，收敛判据是"综合分达标**且** must_fix 数为零"才 ship，轮次耗尽则按 ship_best 取历史最高分那一轮。

**这套机制里有三点值得我们直接学：**其一，综合分由 daemon 重算，模型自己报的分数只作参考，偏差超过 0.01 就发 `composite_mismatch` 警告——**不信任模型的自我评价**，这与我们"gate 用确定性代码"的原则完全一致。其二，权重、阈值、轮次上限、超时全部写在契约文件里而不是提示词里，可配置可审计。其三，多轮不达标时的兜底策略是明确的产品决策（ship_best / ship_last / fail 三选一），而不是让系统悬在那里。

**但它有六个结构性缺陷，恰好定义了 Bifrost 的验证层必须补什么：**

| **缺陷** | **核实到的事实** | **Bifrost 的对策** |
| --- | --- | --- |
| 无视觉回看 | daemon 依赖里没有 playwright/puppeteer；协议是纯文本 XML 标签加 CDATA，没有任何图片承载格式。仓库里的 Playwright 只在 e2e 测试自家 Web UI | Playwright 截图 + 多模态评审作为末道 gate，多视口与中文字体一致性纳入验收 |
| 无维度评数值真实性 | 五个 panelist 全部只评设计（层级、品牌、无障碍、文案）。lint 里的 `invented-metric` 规则只匹配 "10× faster"、"99.9% uptime" 这类营销话术正则，抓不住"图表数字是编的" | 新增**事实评审**作为第一个评审点，并把事实绑定做成确定性 gate 而非评分维度 |
| 修正无法路由 | `MUST_FIX` 是纯自由文本，协议里没有 target 属性，事件契约只有 `{ round, role, text }`。每轮 DESIGNER 重吐整份 HTML，提示词甚至要求"下一轮字节数必须严格小于上一轮"，与增量修补方向相反 | must_fix 必须带 `target=data-bf-card:<id>`，卡片锚点为强制项，修正走局部重写模式 |
| 无真正的回灌 | 编排器只读子进程 stdout，从不写 stdin。三轮"评审"发生在模型的一次流式输出里——它自己扮演五个角色、自己写完三轮、自己决定 ship，daemon 事后重算分数，够不着就把状态标成 `below_threshold`，**但产物已经出来了，不会再要一版** | 评审与制作是两次独立调用，评审结论由服务端回灌并触发重跑；未达红线不得发布 |
| 默认关闭且未接线 | `defaultCritiqueConfig()` 返回 `enabled: false`，四层优先级解析器 `isCritiqueEnabled()` 的文件头注释自述"not yet wired"；更关键的是只有 plain-stream adapter 会走编排器，`claude-stream-json` 等结构化 adapter 一律绕过它走单遍老路径 | 评审是主链路的固定环节，不是可选特性；不依赖执行体的流格式 |
| lint 不阻断且无消费方 | `lint-artifact.ts` 自述"does NOT parse HTML"，是 grep 式检查；保存接口先写盘再 lint，findings 只随响应返回，前端全量 grep 零命中。真正硬阻断的只有查 5 个字面量占位符的 publication-guard，和默认仅 warn 的 stub-guard | lint 改为写盘前校验、DOM 级解析（cheerio）、P0 拒绝 |

**但它的 lint 规则本身很值钱，应当整体移植。**`lint-artifact.ts` 的十五条规则是一套"反 AI 味"的审美底线：紫靛渐变、蓝青双色渐变、`#6366f1` 这类 AI 默认靛色、标题与按钮里的 slop emoji、圆角加左侧色条的卡片、用 Inter/Roboto/system-ui 当展示字体、编造的营销数字、lorem ipsum 占位文案、全大写不加字距、外部占位图 CDN、`:root` 之外裸写超过 12 个 hex、正文中强调色使用超过 6 次。这些规则 Apache-2.0 授权、可直接复制改写，是 Bifrost 视觉底线最省力的来源——我们只需在它之上叠加数据类的六道 gate。
## 三、系统架构

### 3.1 八层结构

```
L0  接入层    数据源连接器 · 文件上传 · 数据库直连 · 飞书多维表格 · API
L1  体检层    Profiling · 质量评分 · 异常与缺失探测 · 治理建议
L2  语义层    metrics.yml 指标口径 · 维度字典 · 岗位画像 · 权限口径
L3  洞察层    AnalysisPlan → QuerySet → FactSet（三段，前两段模型出，第三段纯代码）
L4  叙事层    问题树 → 结论链 → 版式骨架与卡片锚点
L5  制作层    模型手写自包含 HTML + 内联 SVG/ECharts · 事实集数据岛注入
L6  验证层    六道 gate：结构 · 事实绑定 · 口径权限 · 数据断言 · 误导编码 · 截图评审
L7  交付层    飞书推送 · 定时刷新 · 多格式导出 · 订阅与告警
```

L0 到 L2 是"让数据可被信任"，L3 到 L5 是"让分析产生洞察并被表达",L6 是"让产物不出错"，L7 是"让结论到达该看的人"。相对 V2 的调整集中在 L3 拆段、L4/L5 重新划界、L6 gate 表重写。

### 3.2 L3 洞察层拆为三段：模型只碰前两段

V2 把洞察层当成一个整体，导致"模型是否参与算数"这件事含糊不清。V3 明确切成三段，**并规定第三段完全不经过模型**：

**第一段 AnalysisPlan（模型产出）。**回答"该看什么"。输入是业务诉求、岗位画像、数据体检结论、项目记忆里的历史结论与已知异常；输出是一份结构化分析方案：要回答的业务问题清单、每个问题对应的指标与维度切面、需要的对比基准（同比/环比/对标产线/目标值）、预期的分析深度（是否需要下钻归因、是否需要相关性验证）。这一段是纯判断，是"顶级数据工程师"含量最高的地方，也是分析方法 skill 注入的主战场。

**第二段 QuerySet（模型产出，但被白名单约束）。**回答"怎么算"。把 AnalysisPlan 的每个问题翻译成具体查询。关键约束：**SQL 只能引用 metrics.yml 里已定义的指标与维度**，不允许模型自己发明聚合逻辑。想算一个口径里没有的东西，必须先走"提议新增指标"的流程由人确认，而不是就地编一个算法。这条约束是口径唯一性的执行机制——没有它，语义层就只是一份没人遵守的文档。

**第三段 FactSet（纯确定性代码，无模型参与）。**Runner 执行 QuerySet，把结果冻结成一份带版本号的事实集。每条事实的结构是：

```json
{
  "id": "oee.a01.w31",
  "value": 72.3,
  "unit": "%",
  "display": "72.3%",
  "metric": "oee",
  "dims": { "line": "A01", "week": "2026-W31" },
  "definition_ref": "metrics.yml#oee@v3",
  "sql_hash": "a3f1c9…",
  "row_count": 10080,
  "computed_at": "2026-08-04T11:20:00+08:00"
}
```

`definition_ref` 让每个数字可追溯到口径定义的具体版本，`sql_hash` 让它可复现，`row_count` 让样本量不足能被自动发现。事实集是 L4 到 L7 全部环节的唯一数值来源——**制作 agent、评审 agent、交互版渲染器、导出器，四者看到的是同一份事实集**，这是"同一个数在不同地方不一样"这类事故的结构性解药。

### 3.3 六个模型节点与两个评审点

Bifrost 不写自己的 agent loop，而是像 open-design 一样把模型调用委派给可替换的执行体。所谓"agent"在我们这里的准确含义是：**一次带特定系统提示、特定注入知识、特定输出契约的模型调用节点**。V3 收敛为六个节点，划分依据是"产物类型不同"而非"步骤不同"——每个节点的输出是一种可独立校验、可独立重跑、可独立评估的工件。

| **节点** | **产物** | **注入的知识** | **输出契约** | **失败模式与处置** |
| --- | --- | --- | --- | --- |
| 治理 Agent | 体检报告 + 治理提议 | 数据质量规则库、行业常见脏数据模式 | 质量评分卡 + 提议清单（每条含影响面与修复成本） | 误报治理项 → 人工驳回，驳回理由写入项目记忆 |
| 洞察 Agent | AnalysisPlan | 岗位画像、分析方法 skill（损失树、异常检测、归因框架）、项目记忆 | 结构化分析方案 JSON | 方案跑偏 → 事实评审拦下，重跑本节点 |
| 查询 Agent | QuerySet | metrics.yml 全文、维度字典、方言差异说明 | SQL 清单 + 每条的指标引用声明 | 引用未定义指标 → 口径 gate 硬阻断，退回并附可用指标列表 |
| 事实评审 Agent | 事实评审意见 | 岗位画像、业务常识规则、历史口径争议记录 | 通过/退回 + 问题清单（含缺口与可疑项） | 见 3.4 评审点一 |
| 制作 Agent | 自包含 HTML 产物 | 设计系统 tokens、craft 规则、看板模板库、图表片段库、事实集索引 | 完整 HTML（含 data-fact 绑定与 data-bf-card 锚点） | 结构/绑定违规 → 前四道 gate 拦下，带 findings 重跑 |
| 呈现评审 Agent | 呈现评审意见 | craft 规则、可读性规则、岗位画像 | 通过/退回 + must_fix 清单（**每条必须带 repair target**） | 见 3.4 评审点二 |

**关于制作 Agent 为什么不拆成"骨架"和"卡片"两个节点。**V2 曾把它拆为 L4a 编排与 L4b 图表规格两个节点，理由是可以并行生成卡片。V3 合并为一个，原因是：整页一次性手写才能保证视觉整体性——版式节奏、留白呼吸、色彩配比这些是全局属性，分卡片并行生成必然导致拼贴感，而这正是"AI 味"的主要来源。作为补偿，制作 Agent 有两种工作模式：**整页模式**用于首次出稿，**局部模式**用于评审回灌后只重写指定锚点的卡片。这样既保住整体性，又拿到了增量修补的效率，还避免了 open-design 那种"每轮整篇重写且要求越写越短"的退化倾向。

**两个评审点的定位差异必须说清楚**，这是 V3 相对 V2 的实质增量：

**评审点一：事实评审（内容层，L3 之后 L4 之前）。**输入是 FactSet 与 AnalysisPlan，问的是"这些事实真的回答了业务问题吗"。检查项：业务问题是否被完整覆盖、是否存在口径冲突的两个数被并列、样本量是否足以支撑结论、异常值是不是真实业务异常还是数据问题、有没有该看但漏掉的关键切面。**这个评审点在 open-design 里完全没有对应物**——它的五个 panelist 全在评设计，没有任何一维关心内容是否成立。放在制作之前的理由很直白：内容错了，画得再好看也是错的，而且改内容比改产物便宜一个数量级。

**评审点二：呈现评审（呈现层，L5 之后 L7 之前）。**输入是 Playwright 截好的多视口截图加 HTML 源码，问的是"人能不能一眼看懂并信任"。检查项：可读性（对比度、字号、密度）、图表类型是否匹配数据关系、信息层级是否与业务优先级一致、岗位适配度（给厂长的看板是否出现了他无权看的成本数据）、执行细节（对齐、留白、溢出、遮挡）。这一点必须是多模态的，因为溢出与遮挡在源码里看不见。

**关于评审 Agent 的权限边界。**评审 Agent **不能直接改稿**——让评审者动手会同时丢掉独立性和可追溯性。但它**有阻断发布的权力**，且必须把意见分成两档：

- **must_fix（硬阻断）**：口径违规、事实绑定失败、误导性编码、岗位权限越界。未清零不得发布，每条必须带 repair target 指向具体卡片锚点。
- **should_fix（不阻断）**：审美与措辞层面的改进建议。记入 Verify 面板供人决定，不拦发布。

这条设计直接修掉了 open-design 的那个洞：它的 `below_threshold` 只是给运行打个状态标签，产物照样落盘交付。**评审如果不能阻断，它就只是一份没人读的报告。**

### 3.4 Agent 记忆分层：AI 员工而非无状态函数

一次调用无状态、跨调用无记忆的系统只能做工具，做不了员工。顶级数据工程师的价值有很大一部分来自"记得这条产线上个月就出过这个问题"、"记得财务口径和生产口径在这里一直有争议"。Bifrost 的记忆分三层，写入门槛递增：

| **层** | **内容** | **写入方式** | **生命周期** | **对生成的影响** |
| --- | --- | --- | --- | --- |
| 任务记忆 | 本次运行的中间产物、失败重试记录、gate findings | 自动 | 单次运行 | 供本次重跑时避免重复犯错 |
| 项目记忆 | 该客户/产线的历史结论、已知业务异常、口径争议、被驳回的治理提议及理由、岗位偏好 | Agent 自动写入，人可审阅编辑 | 长期，可归档 | 作为洞察 Agent 的先验注入 |
| 组织记忆 | 被验证有效的分析方法、沉淀为 skill 的领域知识 | **人确认后**才入库 | 长期，跨项目 | 升级为 skill 文件，进入全局注入序列 |

**一条不可逾越的红线：记忆只影响判断的先验与优先级，绝不作为数值来源。**"上月 A01 良率 91.2%"这种话不允许从记忆流入产物——需要这个数就重新查一次。这条红线的执行机制不需要额外设计，事实绑定 gate 天然拦住了它：**任何没有对应事实 id 的数字都过不了 L6**，无论它来自模型的想象还是来自记忆。这是把架构约束用在两个地方的一个例子。

组织记忆需要人确认才入库，是因为它会影响所有后续项目——一条错误的"有效方法"污染面太大。项目记忆则允许 Agent 自动写入，因为影响面限于该项目且人可随时编辑，自动化收益大于风险。

### 3.5 L6 验证层：六道 gate

前五道是确定性代码，零模型调用、可单测、可 CI；第六道是多模态模型调用。这个比例是刻意的——**能用代码判定的绝不交给模型判定**，这既是成本考虑，也是可靠性考虑。

| **#** | **gate** | **检查内容** | **实现** | **不通过** |
| --- | --- | --- | --- | --- |
| 1 | 产物结构 | 自包含（无外部脚本/样式/图片 CDN）· 体积上限 · 每个卡片必须有 data-bf-card 锚点 · 数据岛存在且可解析 | cheerio DOM 级解析 | 硬阻断 |
| 2 | 事实绑定 | 页面所有可见数字必须被 data-fact 包裹且 id 存在于事实集；允许的格式化差异（千分位、单位换算、精度）在白名单内 | 正则抽取 + 事实集比对 | 硬阻断 |
| 3 | 口径与权限 | 事实集中每条 SQL 只引用 metrics.yml 白名单指标；卡片字段符合岗位画像的权限口径 | 静态检查 | 硬阻断 |
| 4 | 数据断言 | 行数、空值率、主键唯一性、值域、时间连续性 | pandera 声明式断言 | 硬阻断 |
| 5 | 误导编码 | y 轴截断 · 双轴 · 面积编码放大 · 时间间隔不等 · 饼图类别过多 · 样本量不足仍给强结论 · 色彩对比不足 | 规则集 + Flint IR 校验（可选路径） | 高危阻断，其余告警 |
| 6 | 呈现评审 | 多视口截图的多模态评审，含中文字体一致性 | Playwright + 评审 Agent | must_fix 阻断，should_fix 记录 |

在这六道之上，叠加从 open-design `lint-artifact.ts` 移植改写的十五条反 AI 味审美规则（见 2.4 末段），作为 P1 级告警。**分工是清晰的：移植来的规则管"好不好看"，我们新写的六道 gate 管"对不对"。**
### 3.6 执行形态与分环境安全策略

**执行形态分两类。**需要探索的节点走外部 code-agent CLI——它要翻 metrics.yml、翻 skill 文件、看数据 profile、反复改产物文件，CLI 天生擅长这些，还免费带来文件操作与多轮修正能力。纯判定节点走 API 直调加 JSON schema 强约束——事实评审、口径冲突判断、异常分类这些只需要一次结构化输出，API 更快更省更可控，且能大规模并行。保留 CLI 这一层的真正价值是运行时可替换，能接国内模型与私有部署模型，这个红利不能丢。

**安全不能一刀切。**V2 写了一条"CLI 永不直连数据源"的硬约束，方向对但太粗——它会让开发阶段的迭代效率掉到无法接受。V3 改为按环境分三档，本质是**数据敏感度决定模型能看见什么**：

| **环境** | **数据** | **模型可见范围** | **执行权限** | **审计** |
| --- | --- | --- | --- | --- |
| 开发 / 演示 | 脱敏样例或合成数据 | 可见明细，可直读本地 DuckDB 文件 | CLI 高权限沙箱内自由执行 | 不强制 |
| 预生产 | 真实数据抽样 + 字段级脱敏 | schema + profile + 脱敏样本行 | SQL 经白名单校验后由 Runner 代执行 | 全量记录 |
| 生产 | 真实全量业务数据 | **仅 schema 与 profile**（列名、类型、基数、空值率、极值），不含明细 | CLI 不持任何数据库凭据；查询由 Runner 在独立进程执行；结果按岗位画像过滤后回传 | 全量记录 + 岗位维度留痕 |

生产环境这一档的设计有个额外好处：**它和"模型不写数字"是同一件事的两面。**模型看不到明细数据，也就没有能力编造出一个"看起来对"的数字；它必须走 QuerySet 声明查询、由 Runner 算出事实集。安全约束与可信约束在这里合流，不需要两套机制。

### 3.7 产出物链带来的增量红利

五个中间产物形成一条单向链，每个都落盘、带版本号与输入 hash：

```
Profile → AnalysisPlan → QuerySet → FactSet → HTML产物
```

于是：改岗位画像只重跑洞察及之后；改指标口径只重跑查询及之后；改筛选条件只重跑 Runner 与制作；改设计 token 只重跑制作；评审打回只重写被 repair target 点名的那张卡片。open-design 因为没有中间产物，任何一点改动都要整条重来——**这正是 Bifrost 从"生成器"变成"工作台"的分界线**。

参数面板的即时调参就建立在这条链上，但**边界由事实形状划定**：筛选性参数（事实 id 集合与序列长度不变）可以只换 FactSet 重新注入、不动模型；结构性参数（换指标、换颗粒度、增删维度）改变了事实形状，必须重跑模型——因为产物里的图表是模型手画的 SVG，点数写死在 `<path>` 里，形状变了就必须重画。这两类的判定规则与实现见开发文档 9.9。

### 3.8 技术选型总表

| **环节** | **选型** | **许可** | **选它的理由** |
| --- | --- | --- | --- |
| 计算引擎 | DuckDB（进程内） | MIT | 直读 Excel/CSV/Parquet/数据库；SQL 是文本，可静态解析做白名单 |
| 数据断言 | pandera | MIT | 装饰器式、零基建，比 great-expectations 轻得多 |
| 指标语义层 | 自写 loader + MetricFlow schema 约定 | Apache-2.0（仅 schema） | 避开 dbt/Cube 的重依赖，保留业界通用写法 |
| 产物 lint | cheerio | MIT | DOM 级解析，避免 open-design 那种 grep 式检查的高误报 |
| 视觉回看 | Playwright | Apache-2.0 | open-design 结构性缺失的一环，也是自修正循环的输入 |
| 图表 IR（可选） | microsoft/flint-chart | MIT | **降级为工具而非必经通路**：模板脚手架 + 误导性编码校验对象 |
| 图表渲染 | ECharts / 内联 SVG / 纯 CSS | Apache-2.0 | 由制作 Agent 按场景自选，我们不规定 |
| 交互下钻 | duckdb-wasm 或后端查询接口 | MIT | 仅交互版启用，离线快照版不引入 |
| 生成引擎参照 | nexu-io/open-design | Apache-2.0 | 可修改可闭源分发，独立仓库参照实现而非 fork |
| 模型运行时 | 外部 code-agent CLI / OpenAI 兼容端点 / Aily | — | 可替换层，避免被单一平台或单一模型锁死 |

**相对 V2 的两处选型变化**：Flint Chart IR 从"L4 核心依赖"降为可选工具（理由见 2.2）；新增 cheerio 用于 DOM 级 lint（借鉴 open-design 的依赖选择，但改掉它 grep 式的实现）。Vega-Lite Schema 校验保留但仅在使用 IR 路径时生效。

### 3.9 与既有 DecisionLoop 五层方案的映射

既有材料不浪费，八层是五层的工程化展开：数据可信治理对应 L0–L1，千人千面看板对应 L2–L7 的主链路（原方案只描述了"动态拼装视图"，Bifrost 把它拆成语义层、洞察层、叙事层、制作层、验证层五个可验证环节），智能预警与因果追溯对应 L3 的分析方法体系与 L1 的异常识别，一键消摩协同对应 L7，复盘沉淀对应 3.4 的记忆分层。

## 四、Skill 体系：Bifrost 技能库

这是"高度"最直接的体现：我们要建的不是几条提示词，而是一个可运营、可扩展、可被专家审阅的领域技能库。每个 skill 是一份 SKILL.md，含触发条件、适用指标类型、方法步骤、输出契约与反例清单，可选附带校验脚本。用户目录可影子覆盖内置版本。四类如下。

#### A 类 · 分析方法（决定洞察深度）

OEE 六大损失树分解、帕累托与二八定位、SPC 控制图与 Nelson 判异准则、良率漏斗与工序归因、齐套率与物料缺口传导分析、交付风险的多维度联动（物料/研发/生产/质量/订单五维）、根因假设树与证据链构建、同环比与基准对比的正确姿势、趋势偏移与异常检测、样本量与显著性的基本纪律。

#### B 类 · 可视化设计（决定专业观感）

图表选型决策规则（数据形态到图表类型的映射表）、仪表盘信息层级与视线动线、KPI 卡片与稀疏折线规范、坐标轴与刻度纪律、色彩体系与色盲安全、数字排版与中文对齐、留白与信息密度、误导性图表禁令清单。注意 V3 之后这类 skill 的作用发生了变化：**它不再是渲染器的配置，而是注入给制作 Agent 的判断依据**——它约束的是模型的选择，不是渲染器的能力。

#### C 类 · 叙事与岗位（决定是否可用）

结论先行与金字塔原理、SCQA 问题框架、经营层摘要规范（一个数一个建议，caveat 进附录）、厂长班次简报、供应链缺口简报、设备停机工单叙述、Dashboard Readme 白话说明书生成。

#### D 类 · 数据治理（决定地基）

六类缺陷识别规则、可信度评分算法、假异常与业务例外识别、口径冲突仲裁规范（例如示例数据中 OEE 原值与三率复算不一致该如何处置与呈现）、缺失值处理决策树、单位与时区一致化。

#### 开源资产盘点结论

结论先行：可以直接复制改写的高价值资产几乎全部来自 open-design 自身（Apache-2.0），外部数据分析 skill 仓库大多没有可用许可证，只能吸收分类法与思路、文字必须重写。

| **来源** | **许可** | **可复用内容** | **用法** |
| --- | --- | --- | --- |
| open-design skills/live-dashboard | Apache-2.0 | 看板生成清单，含 P0 级 "Honesty" 段 | 直接移植并加严为事实绑定硬约束 |
| open-design skills/data-report | Apache-2.0 | 数据报告结构与叙事骨架 | 改写为 C 类叙事 skill |
| open-design lint-artifact.ts 十五条规则 | Apache-2.0 | 反 AI 味审美底线（渐变、emoji、字体、占位文案等） | 整体移植为 P1 告警，实现改为 cheerio DOM 级 |
| open-design critique 模板五维 | Apache-2.0 | 评分纪律：引用证据、取最差区间、不许求平均、总均分 >8 视为可疑 | 纪律条款直接照搬，维度改写为口径/图表/洞察/行动/岗位 |
| open-design Design Jury 契约设计 | Apache-2.0 | 权重与阈值写进契约、综合分由服务端重算、多轮兜底策略三选一 | 作为呈现评审的工程设计参照 |
| open-design craft/ 六个文件 | Apache-2.0 | anti-ai-slop、accessibility-baseline、typography-hierarchy、color、state-coverage、laws-of-ux | 整体移植为 B 类可视化设计 skill 基座 |
| open-design last30days/scripts | Apache-2.0 | 确定性数据管线的现成范例 | 作为 Runner 的实现参考 |
| dbt MetricFlow schema | Apache-2.0 | 指标 YAML 的四类型与 type_params 约定 | 只抄 schema 设计，自写 loader |
| microsoft/flint-chart | MIT | Chart IR、语义类型体系、校验工具 | 可选工具：模板脚手架与编码校验 |
| pandera / DuckDB / cheerio / Playwright | MIT / Apache-2.0 | 断言、计算、解析、截图 | 直接依赖 |

**法务红线（必须在写第一行代码前落到 CI 里）：**anthropics/skills 的 LICENSE 明确禁止复制、衍生与分发，只能阅读参考，一个字都不能抄；nimrodfisher/data-analytics-skills 没有 LICENSE 文件，等同保留全部权利，只能吸收分析方法分类法、文字全部重写；WrenAI 为非标准自定义许可、Lightdash 的 ee/ 目录为商业许可、soda-core 为 Elastic License 2.0，三者均不得混入我们的仓库。建议在 CI 增加许可证审计——这一条在企业客户尽调时会被直接问到。
## 五、飞书融合：三个角色，不要混

飞书在 Bifrost 里同时扮演三个角色，方案里必须分清，否则实现时会互相绑死。本节内容已按飞书开放平台官方文档逐项核对，能力边界与配额是实测口径而非假设。

#### 角色一 · Aily 作为可选自然语言入口，不是编排层

**Aily 不能承担编排。**原因有三：其 aily-v1 开放接口只允许自建应用调用，不支持商店应用分发；其内置"数据分析"技能的输入输出 schema 为空，无法作为可编程组件被我们的链路调用；且能力受套餐限制。因此编排必须留在 Bifrost 自己的服务端，Aily 的正确定位是对话入口——用户在飞书里说"看下昨天 A01 线怎么样"，由 Aily Workflow 的 HTTP 节点把请求转给 Bifrost，八层链路在我们这边跑完，再把成品推回会话。这样做同时保住了企业侧的合规诉求与产品的可移植性：Aily 与本地模型、云端模型是同一层的可替换项，任何一个不可用都不会让产品停摆。

#### 角色二 · 多维表格作为数据源与人机协同台

多维表格双向使用：读侧作为 L0 数据源接入原始业务表；写侧把治理结果写回——可信度评分、待确认队列、AI 检测类型与人工确认结论。业务人员在自己熟悉的多维表格里点确认或修正，这是天然的人机协同界面，不需要我们再造一套审核 UI。示例数据包 05 表里那几个"待填"字段正是为这条链路预留的。

**实现约束（已核对）：**批量接口 batch_create / batch_update 单次上限 1000 条记录，需要自己做分批与重试；接口频控不接受提额申请，必须按额度设计写入节奏；要接收记录变更事件必须先完成云文档事件订阅配置，且公式字段的变化不会触发事件——依赖公式列做触发器会静默失效；读写需要双身份（应用身份与用户身份）对应的不同权限范围。

#### 角色三 · 飞书作为交付与协同通道

看板的宿主选网页应用（H5）：它有公开可访问地址、可动态更新而不必发新版本、支持 requestAccess 免登获取用户身份用于岗位鉴权，首次上架需管理员审核。这是三种候选宿主里唯一能同时满足"动态数据"与"岗位权限"的方案——云文档小组件无法操作 iframe，文档内嵌网页块限制 3 QPS，都不适合承载看板。

消息卡片承载"结论加关键数字加一键跳转看板加一键建任务"，采用卡片 JSON 2.0；交互回调 card.action.trigger 必须在 3 秒内响应且不得返回 3xx，卡片更新 token 有效期 30 分钟、最多使用 2 次——这三条直接决定了推送服务必须异步化：回调里只做入队与即时确认，真正的重算放到后台。处置任务用任务 v2 接口跟踪，用 custom_complete 深链把"完成"按钮指回 Bifrost 的处置页；敏感数据走审批 v4，系统只提示"存在该数据"，一键发起申请，通过后才可见；白话说明书与复盘记录沉淀到云文档与知识库。

**一个必须提前处理的平台限制：**机器人发出的消息不会触发另一个机器人，所以"Aily 回复触发 Bifrost 推送"这类链式设计走不通，跨机器人协作必须走服务端直连。此外企业数据不得裸放公网，H5 必须做鉴权；应用可用范围的变更需要走版本审核，这一步有周期，权限申请要在 S0 就并行发起。

#### 接口优先级与前置准备

| **优先级** | **能力** | **接口/形态** | **关键约束** |
| --- | --- | --- | --- |
| P0 | 看板宿主与岗位鉴权 | 网页应用 H5 + requestAccess | 首次上架需管理员审核 |
| P0 | 治理结果写回与人工确认 | Bitable batch_create / batch_update | 单次 1000 条，频控不可提额 |
| P0 | 结论推送 | 消息卡片 JSON 2.0 | 回调 3s 内响应，token 30 分钟/2 次 |
| P1 | 处置任务跟踪 | Task v2 + custom_complete | 深链回 Bifrost |
| P1 | 敏感数据申请 | Approval v4 | 通过后才解密可见 |
| P2 | 自然语言入口 | Aily Workflow HTTP 节点 | 仅自建应用，编排不外放 |
| P2 | 知识沉淀 | 云文档 / 知识库 | 文档内嵌网页块 3 QPS |

## 六、工作台 UI：整体照搬 open-design 外壳

**布局、交互、视图切换、流式输出、设置弹出方式全部照搬 open-design，不自己设计。**这些是它迭代几十版磨出来的，自创只会更差，而且这不是我们的差异化所在——我们的差异化在数据可信度，不在窗口怎么摆。

外壳是**对话栏 + 工作区两块可拖拽分栏**（默认对话栏 460px，可拖到 345–720，可一键折叠为 0 进入专注模式），工作区顶部是一条标签页栏，数据源树、溯源、验证、记忆都是标签或按需唤出的面板，不占常驻栏位。开发文档第 9 章记录了源码级核实结果与全部实现常量，照着写即可。

> 早期版本这里写的是"三栏加时间线"，那是从架构层面推的，与 open-design 实际实现不符（它没有常驻左右栏，时间线也不是独立区域而是对话栏内的消息流）。以开发文档第 9 章为准。

#### 生成过程时间线（透明度即信任）

把 plan、query、fact、make、verify 五个阶段的事件流实时展示在对话栏消息流里，每一步都可展开看中间产物：Agent 制定的分析计划、生成的 QuerySet、Runner 执行的查询与结果集行数、冻结的事实集、六道 gate 的逐项结果、两个评审点的意见与打回记录。open-design 用 SSE 流展示生成过程并把连续同名工具调用折叠成分组卡片，我们复用同一套渲染，把它升级成可审计的过程记录——这既是调试工具，也是答辩时最有说服力的部分：评委能亲眼看到系统在自我检查，包括看到它拦下了自己的错误。

#### 三个差异化组件

**溯源检查器 Provenance Inspector。**看板上任意一个数字都可以点开，弹出它的事实 id、来自哪个数据集、经过哪些过滤与聚合、用的哪条指标定义的哪个版本、SQL 指纹、样本行数、源数据可信度。这是事实集设计在界面上的直接表达，一个动作就能让人相信整套系统。

**验证面板 Verify Panel。**把六道 gate 的结果以红黄绿明示在看板侧边，包括被拦截并修正过的问题记录、两个评审点的 must_fix 与 should_fix 清单。承认系统拦下过自己的错误，比声称从不出错可信得多。

**记忆面板 Memory Panel。**展示本项目的项目记忆条目——历史结论、已确认的业务例外、口径争议裁决、被驳回的治理提议。用户可以直接编辑或删除。这是"AI 员工"这个定位唯一能被用户感知到的地方：他能看见系统记得什么，并且能纠正它。

#### 其余照学

参数面板即时调参（筛选性参数不重跑模型，结构性参数如换指标/换颗粒度仍须重跑——边界由事实形状划定，见开发文档 9.9；即便如此仍比 open-design 彻底，它改任何参数都要重新生成）、iframe 保活池避免切标签重载看板、单一设置对话框加 section 参数的弹出方式、`+` 号标签启动器注册表、技能与设计系统市场一键使用、新建入口按意图分 Tab（新建看板 / 实时看板 / 分析报告 / 治理工作台）、多格式导出（HTML、PDF、PPTX，另加飞书卡片与多维表格）。Studio 画布手动微调列为 P2，它与品牌/设计系统耦合较深。

**明确不抄**：品牌系统、媒体后端（图/视频/语音）、Figma 导入、桌面宠物、社区模板市场、协作邀请、计费。这些是 open-design 的产品面，与数据看板无关。

## 七、实施分期

分期原则是按架构层的依赖顺序推进，每期都必须产出可演示物与可量化指标，不做纯内部重构的迭代。

| **阶段** | **周期** | **交付内容** | **验收物** |
| --- | --- | --- | --- |
| S0 地基 | 3 天 | Dataset 注册与 schema 推断；09_字段字典 翻译为 metrics.yml；**FactSet 的 JSON Schema 定稿；数据岛注入器与事实绑定 gate**；一套数据场景 design tokens | 一份真实数据驱动、每个数字可点开溯源的单卡片 |
| S1 主链路 | 1 周 | L3 三段 → L4 → L5 → L6 垂直打通，单岗位（厂长）；前五道 gate 上线；制作 Agent 整页模式跑通 | 一份全链路可信的 HTML 看板 + Verify 面板 |
| S2 千人千面与治理 | 1 周 | 三岗位路由与权限颗粒度；治理 Agent 跑 05 注错集；待确认队列；项目记忆写入与展示 | 三份看板 + 治理准确率实测数字 |
| S3 双评审与视觉闭环 | 1 周 | 事实评审与呈现评审两个节点；Playwright 截图多模态；repair target 路由与制作 Agent 局部模式；工作台外壳照搬 open-design 与溯源检查器 | 评审打回并自动修正的案例回放 |
| S4 飞书融合 | 1 周 | 网页应用 H5 宿主与免登岗位鉴权、多维表格读写、消息卡片推送、审批与任务、Aily 作为对话入口 | 飞书内端到端演示：一句话到三份看板到岗位推送 |
| S5 打磨与加分 | 1 周 | Studio 手改、更多 design system、PPTX/PDF 导出、对比实验、演示脚本 | 专家盲评对比结果 + 答辩演示流程 |

**关键路径提示：**S0 的 FactSet Schema 与 metrics.yml 是全部后续工作的前置，这三天的质量决定整个项目的上限，不要压缩。相对 V2 的变化是 S0 不再需要定稿 Dashboard Spec Schema（那条路已撤销），省下的时间用于把数据岛注入器与事实绑定 gate 提前到 S0——**因为在 V3 里，这两件东西才是"模型自由发挥但数字可信"的技术支点，必须最早验证。**飞书集成放在 S4 是因为它的不确定性来自外部（权限审批周期、接口配额），不应该阻塞核心能力的验证；但权限申请动作要在 S0 就并行发起。

## 八、评测体系：如何证明"媲美顶级数据工程师"

这是整套方案里最容易被忽略、但决定说服力的部分。专业性必须被测量，否则只是形容词。

| **指标** | **定义与测法** | **目标值** |
| --- | --- | --- |
| 治理准确率 | 用 05 注错集算 precision/recall/F1，分缺陷类型拆开看 | F1 ≥ 0.9，高危类型召回 100% |
| 事实绑定通过率 | gate 拦截数与逃逸数（人工抽检 100 个数字） | 逃逸率 0 |
| 口径一致率 | 产物引用指标与 metrics.yml 定义一致的比例 | 100% |
| 图表恰当性 | 规则 lint 通过率 + 专家盲评评分 | 盲评不低于人工基线 |
| 洞察可行动性 | 带行动建议且有证据绑定的结论占全部结论比例 | ≥ 70% |
| 岗位越权次数 | 看板中出现该岗位无权字段的次数 | 0（安全红线） |
| 评审有效性 | 呈现评审 must_fix 中经人工复核确属真问题的比例 | ≥ 80%（低于此说明评审在制造噪音） |
| 视觉多样性 | 同一份数据不同诉求下产出版式的差异度（人工判定） | 显著区别于模板化输出 |
| 端到端时长 | 从数据接入到三份看板产出的墙钟时间 | ≤ 10 分钟 |

其中"评审有效性"与"视觉多样性"是 V3 新增的两项，分别对应本版的两处核心改动：评审要能阻断就必须先证明它判得准；表达自由度放开了就必须证明它真的产生了多样性，而不是换个写法继续输出同一套模板。

**对比实验（方案的杀手锏）：**同一份数据、同一个诉求，三条路径产出看板——Bifrost、直接让通用大模型生成、人工数据工程师，交给不打标签的专家盲评。这个实验是"媲美顶级数据工程师"这句话唯一站得住的证据形式，建议在 S5 完成并作为答辩核心材料。

## 九、代码复用策略：借鉴而不做下游

明确建议不 fork 它的单体仓库。理由有三：上游平均十一天一个版本、迭代极快，长期合并成本高；我们要改的恰好是它的核心假设（数值可编造、自检不阻断、评审无法路由），改动会集中在最容易冲突的位置；它是 Electron 桌面加本地回环凭据的形态，与我们需要的服务端多用户看板形态不兼容，鉴权体系要重做。本轮源码核实还发现一条新理由：它的 Design Jury 与结构化 stream adapter 不兼容（用 claude-stream-json 会直接绕过编排器），说明其评审链路与执行体耦合较深，我们如果 fork 会继承这个耦合。

正确做法是建独立仓库，把它当参照实现：沿用它的目录约定与架构分层（design-systems、skills、design-templates、runtimes 描述文件、daemon 服务边界），按需移植具体模块的实现——Apache-2.0 允许修改与闭源分发，只需保留版权与 NOTICE 声明并注明变更。品牌名与 logo 属于另一层，产品必须自己的名字，Bifrost 正好。

## 十、风险与对策

| **风险** | **对策** |
| --- | --- |
| 放开模型手写产物后，质量方差变大、偶发翻车 | 下限由模板库与移植来的十五条反 AI 味规则托住，上限由 skill 注入抬高；六道 gate 兜底；模板是起手式不是牢笼 |
| 产物不可复现、不可单测 | 可测边界从"Spec 层"移到"事实集层"：FactSet 完全确定性、可写回归测试；产物层用 gate 与截图基线做回归 |
| LLM 仍可能在文案里编造数字 | 事实绑定 gate 硬阻断：没有 data-fact 包裹的数字一律拦截，模型手上根本没有数字 |
| 评审 Agent 误判导致无谓阻断 | must_fix 限定在四类可客观判定的问题（口径、绑定、误导编码、越权），主观审美只进 should_fix；评审有效性纳入评测指标 |
| 外部 code-agent 以高权限运行，接企业数据是安全面 | 按环境分三档数据可见性；生产环境模型只见 schema 与 profile，不持凭据，查询由 Runner 代执行并全量审计 |
| 项目记忆积累错误结论并自我强化 | 记忆只影响先验不作为数值来源（由事实绑定 gate 天然保证）；项目记忆人可编辑；组织记忆必须人确认才入库 |
| 飞书接口权限与配额的外部不确定性 | 权限申请在 S0 并行发起；集成层做适配器，接口不可用时降级为链接分享 |
| 演示数据中有团队模拟部分 | 产物中标注数据性质（公开真实/团队模拟），答辩时主动说明，反而体现严谨 |
| 专家知识库初期单薄，洞察深度不足 | A 类 skill 先覆盖歌尔核心场景（OEE、良率、齐套）做深而不做广；建立专家评审机制持续补充 |
| 抄错仓库导致许可证污染 | CI 增加许可证审计；anthropics/skills 只读不抄，无 LICENSE 的仓库只吸收分类法并重写文字，非标准许可一律不引入 |
| 把 Aily 当编排层导致产品被平台绑死 | Aily 仅作可选自然语言入口，编排留在自有服务端；Aily 不可用时降级为 Web 入口 |
| 飞书接口频控不可提额，写回量大时被限流 | 按 1000 条批量上限设计分批与退避重试；治理结果异步写入，卡片回调只入队不重算 |

## 十一、一句话总结

Bifrost 吸收 open-design 的核心思想——把专家判断外置成可评审文件、让模型在约束中自由创作——并补上它在数据领域缺失的三层地基：口径唯一的语义层、数值可溯源的事实集、能真正阻断发布并路由到具体卡片的评审层，再通过飞书把成品送到每个岗位面前。

V3 相对 V2 最重要的一次认知修正是：**可信不应该靠限制模型的表达能力来换取。**限制它能用什么数，而不是限制它能画什么——前者让产物可信，后者只会让产物平庸。

它与市面上所有"AI 生成看板"的差别可以用一个动作说明：点开看板上任意一个数字，都能看到它从哪来、用哪个版本的口径算的、样本多大、可信度多少。
