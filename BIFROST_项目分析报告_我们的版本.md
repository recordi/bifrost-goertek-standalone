# BIFROST 项目分析报告

## 面向制造业的数据洞察、岗位看板与 AI 决策闭环

**报告版本：** v1.1  
**报告日期：** 2026-08-16  
**项目名称：** BIFROST  生产经营数据智能分析工作台  
**适用范围：** 本项目自身实现、联合 UI、数据治理与自动适配链路，以及经审核接入的专业分析能力

---

## 一、执行摘要

BIFROST 是一个面向制造业的 AI 数据分析与决策支持工作台。它将数据接入、字段理解、数据质量检查、指标计算、岗位化看板、AI 问答、证据追溯和人工确认组织成一条完整链路。

AI 负责业务理解、任务拆解、分析规划和结果解释；确定性计算负责生成业务数字；证据链负责支撑结论；涉及高风险的动作进入人工确认草稿流程。

项目的核心价值是：

1. 数据来源变化时，可以通过数据档案和字段映射适应不同表结构；
2. 同一套数据可以按厂长、线长、质量、设备、工艺、供应链等岗位生成不同视图；
3. 用户不仅能看到指标，还能从指标下钻到产线、班次、停机、不良、物料、冻结和处理状态；
4. 用户可以通过自然语言请求一个预先没有配置的分析视图，例如“查看三号产线最近 30 个班次的 OEE 趋势并说明下降原因”；
5. 联合专业分析能力遵循统一任务合同、证据门禁和只读边界，作为可审查的分析能力进入主流程。

本报告严格区分三种状态：

| 状态 | 含义 |
|---|---|
| 已实现并验证 | 已有代码、接口或测试记录支持 |
| 演示可用 | 可在本地演示，但仍使用脱敏或团队模拟数据 |
| 待接入/待确认 | 需要真实企业数据、人工映射确认或后续部署能力 |

---

## 二、业务背景与问题定义

歌尔等大型制造企业通常同时使用 ERP、MES、QMS、WMS、PLM、CRM 等系统。数据分散会同时增加查询成本、异常判断难度和跨部门协同成本。

当前典型问题包括：

- 生产、质量、设备和供应链数据分散在不同系统，无法快速联动；
- 临时报表依赖 IT 排期，业务人员无法自主获得适合岗位的分析；
- 异常通常在日报或周报中才被发现，处置滞后；
- 缺失值、重复记录、异常值、格式冲突、逻辑冲突和时效滞后往往上线后才暴露；
- 管理层需要全局风险，一线人员需要班次和工序细节，统一报表无法同时满足；
- 只有指标没有原因，业务人员无法判断“为什么发生、影响什么、下一步谁处理”。

因此，BIFROST 建立：

> 数据进入后能够被理解、被验证、被解释，并最终转化为岗位可执行的决策信息。

---

## 三、产品定位与用户对象

### 3.1 产品定位

BIFROST 是“数据接入 + 多智能体分析 + 可解释看板 + 协同闭环”的工作台，覆盖从数据进入到决策复盘的完整过程。

### 3.2 六类岗位视图

| 角色 | 主要问题 | 典型决策 |
|---|---|---|
| 厂长 | 哪些产线、环节和风险最需要关注 | 是否调整资源、排产或升级处理 |
| 线长 | 本线当前状态、班次波动和异常原因是什么 | 是否停线排查、调整当班安排 |
| 质量 | 不良来自哪里、是否需要冻结或复检 | 是否启动隔离、复检或整改 |
| 设备 | 停机由什么造成、维修影响多大 | 是否安排检修、备件或设备排查 |
| 工艺 | 工艺是否稳定、换产是否超时 | 是否补采 SPC 数据、调整工艺参数 |
| 供应链 | 缺料、冻结物料是否影响交付 | 是否催料、替代、调整交付计划 |

六个角色共享同一数据底座，可以指向同一条产线、同一个班次或同一个事件，同时按照岗位职责呈现不同证据和决策动作。

### 3.3 技术设计与用户需求对应关系

项目中的每项技术设计都对应一个明确的使用需求，用户可以在页面上看到对应结果：

| 用户需求 | 用户实际看到的功能 | 技术实现 | 验收方式 |
|---|---|---|---|
| 我想确认数据可用性 | 数据来源、数据范围、检测状态、影响说明 | 数据源档案、六类质量规则、能力清单 | 显示已检测项、待补充项和数据性质 |
| 我的 Excel 字段名称不一样 | 字段映射建议、置信度、确认入口 | 语义映射、字段类型推断、映射确认合同 | 两套结构不同的数据均能生成映射草稿 |
| 我想看可信的 OEE | OEE、开动率、性能率、良品率及目标差距 | 统一指标口径、确定性计算、值域校验 | 结果带规则版本、样本量和证据 |
| 我想知道问题原因 | 停机原因、不良类型、物料缺口和影响排序 | 生产/质量/设备/供应链分析 Skill | 从汇总图进入原因明细和关联记录 |
| 我想定位到具体班次 | 产线、班次、日期、工序和处理状态 | 统一数据集、维度索引、事件范围门禁 | 点击产线或异常项后范围保持一致 |
| 我想让不同岗位看不同内容 | 六类角色视图和角色专属动作 | 角色画像、权限矩阵、角色投影 | 同一事件在不同角色下显示不同重点 |
| 我想临时查看一个趋势 | AI 生成指定时间窗、产线和指标的临时视图 | AI 问题解析、查询计划、动态载荷 | 问题中的角色、范围、时间窗进入请求合同 |
| 我想知道 AI 为什么这样判断 | 结论、影响、证据、数据缺口和下一步 | 业务解读层、证据引用、事实审查 | 每条正式结论可以展开证据与缺口 |
| 我想修改计算标准 | 规则试算、影响预览和审批草稿 | 受限公式引擎、规则版本、基线哈希 | 试算只读，审批前后版本可追溯 |
| 我想确认高风险动作 | 待确认动作、责任角色、确认状态 | 人工确认合同和动作权限门禁 | 解除冻结、排产调整等动作进入人工队列 |

这张对应表是项目的核心阅读入口：技术模块以用户任务为目标，用户界面以业务语言呈现，审计详情再提供字段、规则和证据定位。

---

## 四、实际产品链路

```text
上传数据
  ↓
生成数据源档案
  ↓
字段映射草稿与质量检查
  ↓
发现表间关联和可计算能力
  ↓
生成统一数据集与指标事实
  ↓
按角色、产线、时间范围生成看板
  ↓
指标下钻到班次、原因、记录和证据
  ↓
AI 问答生成临时分析视图
  ↓
形成解释、建议动作和人工确认事项
  ↓
复盘处理结果
```

### 4.1 数据源档案

系统首先记录数据来源、文件指纹、表数量、字段结构、时间范围、产线维度和数据性质。

数据性质必须明确区分：

- 企业官方脱敏数据；
- 团队工程化模拟数据；
- 本地演示数据；
- 尚未完成映射确认的数据。

### 4.2 字段映射与数据治理

字段名称相似时，系统先生成映射草稿，并给出置信度、依据和待确认项。映射确认流程包含以下结果：

- 已确认字段进入正式指标计算；
- 缺失字段进入“待补充数据”清单；
- 未知字段进入“待确认映射”清单；
- 所有未完成项同时展示影响范围和下一步处理建议。

治理结果按业务语言展示“问题—影响—建议动作—处理状态”，原始字段名、规则编号和证据引用放在技术详情中。

### 4.3 统一指标与事实

OEE、开动率、性能率、良品率、不良数、停机时长、换产超时、物料缺口和冻结数量等指标由确定性程序计算。

每个事实至少保留：

- 数据源标识；
- 表和记录定位；
- 指标口径版本；
- 计算方式；
- 时间范围；
- 样本量；
- 证据引用；
- 生成时间和运行标识。

模型基于事实进行解释，所有进入看板的数字均关联数据来源、口径和证据。

### 4.4 一个业务问题如何被系统处理

以用户提问“查看三号产线最近 30 个班次的 OEE 趋势，并说明下降原因”为例：

| 处理阶段 | 系统动作 | 用户得到的结果 |
|---|---|---|
| 识别问题 | 识别产线、时间范围、指标和“原因分析”意图 | 请求上下文显示“三号产线 / 最近 30 个班次 / OEE” |
| 制定分析计划 | 编排生产、设备、质量和供应链相关任务 | 系统知道需要同时检查停机、不良、物料和换产 |
| 读取事实 | 按当前产线和时间窗读取已确认数据 | 生成 OEE 趋势、目标差距和样本量 |
| 分析原因 | 分别计算停机影响、不良影响和供应影响 | 输出主要影响项、影响程度和关联记录 |
| 证据审查 | 检查每项结论的来源、范围和数据完整性 | 结论标注“已核验”“待补数据”或“暂无法判断” |
| 业务解读 | 将技术结果翻译为业务语言 | 说明发生了什么、影响什么、建议谁处理 |
| 看板呈现 | 生成趋势图、原因排序、明细入口和待办卡片 | 用户可以继续点击到班次、原因、记录和处理状态 |

当数据只支持 OEE 计算、缺少设备故障码时，页面会同时给出 OEE 趋势和“设备根因暂无法判断”的说明，并列出需要补充的故障记录。这样用户能区分已确认事实、关联线索和待补数据。

---

## 五、多智能体与 Skill 协作设计

### 5.1 主流程智能体

| 智能体 | 输入 | 输出 | 权限边界 |
|---|---|---|---|
| 编排智能体 | 用户问题、角色、范围、时间窗 | 任务计划和依赖关系 | 负责分析流程编排 |
| 数据治理智能体 | 数据源档案和质量规则 | 缺陷、影响和治理建议 | 不修写原始数据 |
| 映射智能体 | 字段名、类型、样例、关联线索 | 映射草稿和确认清单 | 低置信度必须人工确认 |
| 生产分析智能体 | 产量、OEE、停机和班次事实 | 生产影响与原因分析 | 使用权威事实进行分析 |
| 质量分析智能体 | 良率、不良、冻结、复检事实 | 质量定位与风险建议 | SPC 数据不足时不计算 Cpk |
| 设备分析智能体 | 停机、设备、故障和维修事实 | 设备影响和排查建议 | 缺少设备级记录时明确缺口 |
| 工艺分析智能体 | 换产、工艺参数和 SPC 事实 | 工艺稳定性与换产分析 | 不用缺失数据宣称过程失控 |
| 供应链分析智能体 | 订单、库存、到货、冻结事实 | 缺料和交付影响 | 只有关联证据充分时形成事件结论 |
| 业务解读智能体 | 已核验分析结果 | 业务语言结论、影响和下一步 | 不新增事实数字 |
| 证据审查智能体 | 结论、事实和引用 | 通过、待补证据或阻断 | 可阻断不可信结论 |

### 5.2 联合专业分析能力的接入方式

联合方案复用的内容主要包括专业分析 Skill、UI 基线和部分分析执行能力。它们在本项目中的位置是：

1. 通过统一任务合同转换成 BIFROST 可识别的任务；
2. 以当前角色和产线范围接收输入；
3. 输出结构化结论、指标、原因、证据和数据缺口；
4. 经过证据门禁和只读边界检查；
5. 通过审核的结果作为“已核验派生结论”进入业务层；
6. 证据不足的结果进入“待补充数据”或“辅助审查”状态，并展示缺口和补数建议。

融合方式是把可复用的分析方法纳入 BIFROST 的任务编排、证据检查和角色解释链路。

---

## 六、角色看板与下钻设计

### 6.1 厂长视图

首屏回答“全厂哪里需要关注”：

- 全厂 OEE、产量、开动率、良品率；
- 多产线趋势和排名；
- 产线状态、风险等级和待确认事项；
- 生产、质量、设备、供应链四类影响摘要。

下钻链路：

```text
全厂摘要 → 产线排名 → 目标产线 → 班次趋势 → 停机/不良/物料 → 具体记录与动作
```

### 6.2 线长视图

- 当前产线当班状态；
- 最近 7 班次和最近 30 班次趋势；
- 产量、OEE、开动率、性能率、良品率；
- 停机明细、不良类型、换产超时；
- 需要线长确认的排查和排产动作。

### 6.3 质量视图

- 良品率、一次合格率、不良数；
- 不良类型及累计影响；
- 产品、工序、班次和产线分布；
- 冻结批次、待复检数量、确认状态；
- SPC/Cpk 数据是否足以判定。

### 6.4 设备视图

- 设备可用率和停机总时长；
- 停机原因及累计影响；
- 故障、维修和恢复记录；
- MTBF、MTTR 是否可计算；
- 需要设备人员确认的排查事项。

### 6.5 工艺视图

- 性能率、换产时长和换产超时；
- 工艺参数趋势；
- SPC 原始测量点、规格限和抽样规则是否齐全；
- 工艺稳定性判断及待补数据。

### 6.6 供应链视图

- 物料缺口数量和物料项数；
- 采购数量、到货数量和库存水位；
- 质量冻结对可用库存的影响；
- 受影响工单和交付承诺；
- 待人工确认的催料、替代或交付调整动作。

---

## 七、AI 问答与临时看板

AI 助手不仅返回文字答案，还应该把问题转换为可执行的看板请求。

例如用户输入：

> 查看三号产线最近 30 个班次的 OEE 趋势，并说明下降的主要原因。

系统应自动完成：

1. 识别“产线=三号产线”；
2. 识别“时间范围=最近 30 个班次”；
3. 识别“指标=OEE”；
4. 生成趋势图和对比基线；
5. 调用生产、设备、质量等相关分析任务；
6. 判断停机、不良和其他证据是否足够；
7. 输出“发生了什么、影响多大、可能原因、证据是否充分、下一步做什么”；
8. 提供下钻入口或生成临时视图。

AI 回复必须避免只输出原始字段名、任务 ID、证据哈希和内部规则编号。业务层使用中文解释，技术信息放入可展开的审计详情。

---

## 八、可信计算与安全边界

### 8.1 计算边界

- 正式业务数字由确定性指标服务生成，模型负责提问理解和结果解释；
- 指标计算使用白名单规则和确定性代码；
- 规则试算保持只读，正式发布进入人工审批流程；
- 规则修改生成审批草稿，不静默改写历史事件；
- 派生结论必须保留来源、输入、表达式和版本信息。

### 8.2 证据边界

- 每个正式结论必须绑定当前数据源和当前事件/范围；
- 证据不足时显示“暂无法判断”；
- 产线结论只使用当前产线范围内的证据；
- 相关性结果明确标注为关联线索，并提供进一步验证建议；
- 模拟数据统一标注来源性质，企业真实数据单独登记和授权。

### 8.3 动作边界

解除冻结、排产调整、修改阈值、修改承诺交期和发布知识等高风险动作，只生成待确认草稿，不由系统自动执行。

---

## 九、数据兼容性验证

BIFROST 的兼容性目标是让不同数据集经过同一套流程后，根据自身字段和业务含义生成相应结果。

### 9.1 验证流程

```text
团队模拟数据 / 官方脱敏数据
  ↓
数据源档案
  ↓
字段映射草稿
  ↓
人工确认或继续补充
  ↓
统一数据集
  ↓
可计算能力判断
  ↓
角色化 Overview/Event 载荷
  ↓
专业分析与业务解释
```

### 9.2 安全状态

当映射服务不可用或字段存在歧义时，两套数据保持 `needs_confirmation` 状态；OEE、SPC、MTBF 和供应风险等能力同时显示所需字段、当前缺口和确认入口。

映射确认后，系统才允许生成正式可消费指标；字段缺失的能力仍保持 `not_observable`，并说明影响和下一步补数要求。

### 9.3 数据性质说明

当前数据用于方案验证，包含团队模拟数据和脱敏测试数据，不代表歌尔真实生产数据。正式部署时，数据源注册、权限、规则版本和证据来源必须重新绑定到企业系统。

---

## 十、已完成、可演示与待接入能力

### 10.1 已实现并有回归记录

- BIFROST 项目代码与联合 UI 基线；
- 六角色页面和多产线视图结构；
- 数据源自动适配接口；
- 字段映射草稿和数据治理结果；
- 统一任务合同与只读 Skill 执行边界；
- 证据引用和来源校验；
- OEE、良率、换产等规则只读试算；
- AI 请求的角色、产线、时间范围和事件上下文校验；
- 联合专业分析能力的兼容桥接和回归测试；
- 动态载荷预览和回退机制。

### 10.2 演示可用但仍使用测试数据

- 多产线趋势、排名和下钻；
- 数据治理页面；
- 事件中心；
- AI 助手和临时分析请求；
- 生产、质量、设备、工艺和供应链角色分析。

### 10.3 尚待企业化接入

- 企业真实系统的持续数据连接；
- 完整的 SPC 原始测量点和规格限；
- 设备级故障码、维修工单和设备主数据；
- 真实审批系统和任务系统写回；
- 多用户权限、审计存储和正式部署环境；
- 规则正式发布和版本回滚流程。

---

## 十一、项目价值

### 对管理层

从“看一组指标”升级为“识别风险、查看影响、分配动作”。

### 对一线人员

从“等待日报”升级为“查看当前班次、原因、证据和下一步处理”。

### 对 IT 与数据团队

从“重复开发报表”升级为“维护语义、规则和数据连接”。

### 对企业 AI 建设

从“单次问答”升级为“可追溯、可审查、可复用的智能分析工作流”。

---

## 十二、当前限制与风险

本项目必须主动披露以下限制：

1. 当前验证对象为脱敏或团队模拟数据，企业生产环境需要重新完成数据授权和验证；
2. 数据映射未确认时，系统保持待确认状态并展示影响范围，确认后再进入正式指标链路；
3. 缺少原始测量点、规格限、设备故障或维修数据时，系统不会强行计算对应指标；
4. 联合专业分析能力只有在证据、范围和版本均通过校验后才能影响正式业务解释；
5. 规则修改当前是只读试算和审批草稿，尚未开放自动发布；
6. 最终上线前仍需要企业权限、数据连接、审批和任务系统的正式配置。

---

## 十三、后续建设路线

### 阶段一：可信演示闭环

- 固化两套测试数据的映射确认过程；
- 完成厂长和线长的指标—原因—证据下钻；
- 完成 AI 问答生成趋势和临时分析视图；
- 完成联合专业能力在证据充分时的正式业务呈现。

### 阶段二：专业分析增强

- 增加质量、设备、工艺和供应链的专属证据投影；
- 增加批次、工序、设备、物料和订单的关联下钻；
- 增加解释模板、责任人和动作建议。

### 阶段三：企业系统接入

- 接入 MES、QMS、ERP、WMS 等系统；
- 建立生产环境数据快照和权限控制；
- 接入飞书多维表格、审批、任务和消息卡片；
- 建立规则、知识和组织记忆的正式版本管理。

---

## 十四、结论

BIFROST 的核心价值是把制造业数据从分散、难懂、难追溯的状态，转换成不同岗位可以理解、验证和行动的信息。

项目通过数据适配、确定性指标、统一任务合同、专业 Skill、证据门禁、岗位化视图和 AI 问答，形成了从数据到决策的基础闭环。

最终交付标准是：

> 业务人员能看懂问题，能追溯证据，能判断影响，能知道下一步由谁处理，并且系统不会在数据不足时假装确定。

---

## 附录：复现与审查清单

- [ ] 检查数据源性质和文件指纹；
- [ ] 检查字段映射状态是否为 `approved`；
- [ ] 检查指标是否具备必要输入字段；
- [ ] 检查 Overview/Event 的角色、产线和时间范围；
- [ ] 检查每个正式结论的证据引用；
- [ ] 检查联合专业分析是否通过只读和证据门禁；
- [ ] 检查规则试算是否绑定当前规则版本和数据范围；
- [ ] 检查高风险动作是否仍处于人工确认状态；
- [ ] 检查页面是否使用业务中文解释而非原始字段名；
- [ ] 检查测试数据是否被明确标注为脱敏或团队模拟数据。
