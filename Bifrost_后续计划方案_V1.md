# Bifrost 后续计划方案 V1

> 编制日期：2026-08-07  
> 依据：`Bifrost_完整架构方案_V3.md`、`Bifrost_开发文档_V1.md`、`AUDIT_FINDINGS.md`、`REMEDIATION_PLAN.md`、当前代码与测试结果。  
> 本文先确定后续建设顺序、阶段目标和验收标准；详细文件改动见 `Bifrost_后续开发方案_V2.md`。

> 评审修订：2026-08-07 晚间再次对照当前工作区后，补充“比赛闭环版 / 完整产品版”双里程碑，修正周期、内部文件路径边界、Artifact 版本持久化和真实模型验收方式。当前工作区正在并行开发，实施前必须重新核对文件状态，已存在文件不得重复创建或覆盖。

## 一、计划结论

Bifrost 后续不再继续横向增加功能，先集中完成一件事：

> **把“真实数据导入 → 画像 → 指标口径 → SQL 校验与执行 → FactSet → 双评审 → BI 看板 → 发布交付”打通为一条可重复、可恢复、可审计的主链。**

旧方案中的核心方向继续保留：

- 模型负责分析、叙事和表达，不直接编造业务数字。
- Runner 是唯一的数据执行边界，FactSet 是唯一的业务数字来源。
- `metrics.yml` 是指标口径的唯一事实源。
- Gate 是发布硬门，不能读取失败后静默跳过。
- 看板采用模型自由生成 HTML，再通过事实绑定、截图评审和局部修补保证可信与质量。
- 产品形态继续参考 OpenDesign 的工作台思路，但不复制与数据分析无关的能力。

当前不适合继续投入的方向：

- 新增更多 Agent 名称，但不解决上下游闭环。
- 新增更多导出格式，但发布页和真实看板仍不可用。
- 新增更多数据格式，但连接器、画像和执行器没有统一支持。
- 新增更多 UI 标签，但标签内部仍是占位内容。
- 继续用预制 `Profile`、`FactSet` 或演示数据证明主链可用。

## 二、当前基线

### 2.1 已经具备的基础

- `web -> daemon -> runner` 三服务结构已经建立。
- JSON Schema、TypeScript、Pydantic 三端契约已经形成。
- 数据上传、项目管理、数据画像接口已经存在。
- Insight、Query、Fact Review、Narrative、Maker、Presentation Review 节点已经存在。
- SQL 白名单、FactSet、数据断言、敏感字段过滤模块已经存在。
- Gate 1、2、3、5 和视觉评审框架已经存在。
- HTML、PDF、PPTX 导出和飞书交付骨架已经存在。
- SQLite 表结构、项目记忆、运行记录和审计相关代码已经存在。

### 2.2 必须承认的真实缺口

- 上传数据没有自动进入运行主链。
- `fact` 阶段没有调用已经存在的 Runner `/internal/execute`。
- Runner 执行 SQL 前没有完整注册真实数据集视图并绑定 `{{dataset_id}}`。
- daemon 与 runner 的端口、HMAC、指标路径没有统一配置源。
- Profile 存在多套实现和多种返回结构。
- 前端运行入口仍依赖 `/demo` 数据。
- Narrative 结果没有成为 Maker 的正式输入。
- Fact Review 发现问题后不能驱动 Query 或 Fact 重跑。
- 看板只有整页 HTML，没有稳定的 Dashboard、Page、Widget 和 Filter 模型。
- `/dashboard/:runId` 发布页缺失，飞书卡片中的看板链接可能无效。
- 运行状态、SSE 事件和取消控制主要依赖内存，服务重启后不能恢复。
- 当前 `run_artifacts` 以 `(run_id, kind)` 为唯一键，不能保留多轮修正产生的 Artifact 版本链。
- 当前没有 Dashboard、DashboardVersion、Publication 的持久化表，无法支撑稳定发布和回滚。
- 部分错误被记录为警告后继续执行，审计链可能失真。
- 当前全仓类型检查失败，Runner 全量测试存在顺序依赖。

## 三、后续总目标

### 3.1 产品目标

用户只需要完成以下操作：

1. 创建项目。
2. 上传真实或脱敏数据文件。
3. 选择岗位并输入分析诉求。
4. 等待系统完成分析。
5. 在工作台查看数据画像、事实依据、评审记录和最终看板。
6. 发布为稳定链接，或导出、发送到飞书。

系统必须自动完成：

```text
数据导入
  → 文件校验
  → 数据画像
  → 指标口径加载
  → 分析计划
  → QuerySet
  → SQL 白名单
  → 数据集视图注册
  → SQL 执行
  → FactSet
  → 数据断言
  → 事实评审
  → 叙事计划
  → HTML 看板
  → 确定性 Gate
  → 截图评审
  → 局部修补
  → 发布与交付
```

### 3.2 可信目标

- 最终看板中的业务数字全部来自 FactSet。
- 每个关键数字可追溯到数据集、查询、指标口径和过滤条件。
- 岗位禁止字段不得进入模型上下文、FactSet、HTML 和交付内容。
- 任一硬 Gate 失败时，禁止发布。
- 数据连接、画像、校验、执行、审计任一关键步骤失败时，运行明确失败，不得静默跳过。

### 3.3 工程目标

- 全仓类型检查通过。
- 全仓测试在固定顺序和随机顺序下均通过。
- 存在一个不使用预制 FactSet 的真实端到端测试。
- 服务重启后可以读取历史运行、产物、Gate、评审和事件。
- 取消运行可以真正中断后续模型调用和交付副作用。
- daemon、runner、web 使用统一配置定义。

## 四、实施阶段

## 第一阶段：稳定当前工程基线

**目标：** 当前工作区先达到可继续开发的稳定状态。

### 工作内容

- 修复 daemon、web、runner 的全部类型检查错误。
- 修复 Runner 测试的顺序依赖和全局环境污染。
- 清理测试中被吞掉的原始输出、数据库未初始化和 usage 缺失错误。
- 统一测试临时目录，避免 Windows 权限和中文路径影响测试。
- 修正文档中已经失效的运行命令和阶段状态。

### 完成标准

- `pnpm.cmd typecheck` 通过。
- `pnpm.cmd test` 在允许网络依赖的环境中通过。
- daemon 测试不再输出审计落盘失败警告。
- Runner 测试单独运行和全量运行结果一致。
- 不新增功能，只消除不稳定基线。

### 预计时间

`2` 个工作日。

---

## 第二阶段：打通真实数据主链

**目标：** 完成第一个不依赖演示 Profile 和预制 FactSet 的端到端闭环。

### 工作内容

- 统一 Dataset、Profile、Metrics 的存储与加载方式。
- 上传成功后同步或异步生成标准 Profile，并记录明确状态。
- 运行接口只接收项目、岗位、诉求和数据集 ID。
- daemon 从项目目录和数据库加载 Profile、Role、Metrics。
- 统一 Runner 地址、端口和 HMAC 签名。
- Runner 注册真实数据文件为 DuckDB 视图。
- 数据集执行引用只允许携带项目 ID、数据集 ID 和受控相对路径，Runner 必须再次校验解析后的路径仍位于项目数据目录。
- 执行前完成数据集占位符绑定和 SQL 再校验。
- daemon 的 fact 阶段调用 `/internal/execute` 并保存 FactSet。
- 将数据断言和敏感字段过滤结果写入运行审计。

### 完成标准

- 上传一个 CSV 后，可以直接发起分析并生成 FactSet。
- 运行请求不再接收前端传入的完整 Profile、Role、Metrics。
- 不传预制 FactSet 时主编排正常完成 fact 阶段。
- SQL 中出现未声明数据集、越权字段或禁止函数时运行被阻断。
- 至少有一个真实文件端到端测试覆盖导入、画像、执行和 FactSet。

### 预计时间

`4` 个工作日。

---

## 第三阶段：形成真正的分析闭环

**目标：** Agent 不只是串行调用，而是能够根据问题回到正确的上游节点修正。

### 工作内容

- NarrativePlan 正式进入 Maker 输入契约。
- Fact Review 的问题按类型路由到 Query、Fact 或 Metrics 审批。
- Query 校验失败时只重跑 Query 和后续阶段。
- 数据断言失败时禁止用模型解释掩盖问题。
- Presentation Review 只处理视觉和表达问题，不处理事实问题。
- 统一所有 Agent 的 Runtime Adapter、超时、取消、Token 和错误结构。
- 对运行预算设置明确限制：最大模型调用次数、最大修补轮次和最大执行时长。

### 完成标准

- NarrativePlan 的每个关键章节能在最终看板中找到对应内容。
- Fact Review 能触发一次 Query 修正并重新生成 FactSet。
- Presentation Review 不得修改 FactSet 中的业务值。
- 取消运行后不再产生新的模型调用、截图或飞书请求。
- 每个节点都有输入摘要、输出引用、耗时、Token 和失败原因。

### 预计时间

`4` 个工作日。

---

## 第四阶段：完成 BI 产品模型和工作台

**目标：** 从“整页 HTML 生成器”升级为可持续使用的 BI 看板产品。

### 工作内容

- 建立 Dashboard、Page、Widget、Filter、Layout、PublishedVersion 模型。
- HTML 仍是主要呈现产物，但必须对应一个可解析、可编辑的 Dashboard Manifest。
- 工作台数据标签展示数据集、画像和质量问题。
- 溯源标签展示 Fact → Query → Dataset → Metric 链路。
- 记忆标签接入真实项目记忆接口。
- 实现筛选参数与 FactSet 重算机制。
- 实现 `/dashboard/:runId` 稳定发布页。
- 对外发布使用稳定 `dashboardId`，`runId` 只作为生成来源；旧 `/dashboard/:runId` 仅可作为兼容跳转，不作为长期公开标识。
- 添加发布、取消发布和历史版本查看。
- 前端提供 HTML、PDF、PPTX 导出入口。

### 完成标准

- 用户能够在前端查看真实数据集和画像，不再看到占位内容。
- 用户点击任一关键数字可查看完整溯源。
- 发布链接在服务重启后仍可访问。
- 筛选参数变化会重新执行查询或从允许的事实集合中确定性过滤。
- 飞书卡片中的“查看完整看板”链接有效。

### 预计时间

`5` 个工作日。

---

## 第五阶段：持久化、安全和交付

**目标：** 让系统具备可部署、可恢复和可审计的基本条件。

### 工作内容

- Run、Stage、NodeCall、Artifact、Gate、Review、Event 全量持久化。
- 建立不可变 ArtifactVersion，保存版本号、父版本、内容摘要和生成节点。
- SSE 支持断线重连和事件回放。
- 实现真正的取消信号和阶段幂等键。
- 限制所有导出路径只能位于受控目录。
- 数据集接口不返回服务器绝对路径。
- daemon 增加统一身份认证和项目权限检查。
- 完成飞书 H5 免登、回调验签、岗位映射和 Token 管理。
- Gate 配置读取失败时直接阻断，不允许跳过检查。
- 敏感字段在进入模型前完成过滤，并增加泄漏负例测试。

### 完成标准

- 服务重启后历史 Run、事件和看板可恢复。
- 重复提交同一幂等请求不会产生重复 Run 或重复飞书消息。
- 非项目成员不能读取数据集、FactSet、看板和导出文件。
- 导出接口无法写入项目目录之外。
- 飞书回调无法绕过验签。
- 敏感字段泄漏测试全部通过。

### 预计时间

`4` 个工作日。

---

## 第六阶段：OpenDesign 式产品完成度

**目标：** 补齐工作台的使用体验、技能路由和产物版本能力。

### 工作内容

- 把“岗位 + 数据类型 + 分析诉求”转为结构化 Brief。
- 根据 Brief 自动选择分析技能、叙事技能和视觉技能。
- 引入设计系统、主题 Token 和行业模板的项目级覆盖。
- 支持 Artifact 版本对比和局部回滚。
- 支持用户针对卡片提出修改要求，而不是整页重生成。
- 完成桌面端和移动端布局检查。
- 增加对比评测：Bifrost、通用模型、人工基线。

### 完成标准

- 相同项目可以复用主题、口径和岗位偏好。
- 用户可以只修改一个卡片，并保持其他卡片不变。
- 看板版本可以查看、比较和回滚。
- 盲评中事实正确率、可读性和决策价值达到预设标准。

### 预计时间

`4` 个工作日。

---

## 第七阶段：最终验收与比赛交付

**目标：** 形成可重复演示、可部署、可答辩的最终版本。

### 工作内容

- 固化一套脱敏的歌尔业务样例数据。
- 准备三类岗位的完整演示脚本。
- 记录一次完整运行的所有事件、产物和审计结果。
- 完成部署文档、故障排查文档和答辩材料。
- 执行完整回归、性能测试和安全负例测试。

### 完成标准

- 从上传数据到发布看板可以连续演示成功。
- 全流程不需要手工修改中间文件。
- 任一数字可以现场展示来源。
- 任一 Gate 失败可以现场展示阻断原因。
- 服务重启后看板和运行记录不丢失。
- 三岗位输出有明显差异，但使用相同事实集和指标口径。

### 预计时间

`2` 个工作日。

## 五、里程碑与总体周期

### 5.1 比赛闭环版

比赛闭环版只包含能证明核心价值的必要能力：

- 稳定工程基线。
- 真实数据导入、画像、SQL 执行和 FactSet。
- Fact Review、Narrative、Maker、Gate 和局部修补闭环。
- 最小 Dashboard Manifest、发布页和事实溯源。
- 真实数据链测试和一次真实模型全流程验收。

暂不把完整飞书免登、多租户权限、复杂筛选器和完整版本管理界面作为比赛闭环的阻塞条件。

预计：`15` 个工作日。

### 5.2 完整产品版

在比赛闭环版基础上继续完成：

- 完整运行恢复和事件回放。
- Artifact 与 Dashboard 多版本、对比和回滚。
- 身份认证、项目权限和飞书真实交付。
- 完整筛选、订阅、岗位模板和设计系统覆盖。
- 全量安全负例、性能和稳定性验收。

预计再投入：`15` 个工作日。

### 5.3 阶段估算

| 阶段 | 目标 | 时间 |
| --- | --- | ---: |
| 第一阶段 | 稳定工程基线 | 2 天 |
| 第二阶段 | 打通真实数据主链 | 4 天 |
| 第三阶段 | 分析与多 Agent 闭环 | 4 天 |
| 第四阶段 | BI 产品模型与工作台 | 5 天 |
| 第五阶段 | 持久化、安全与交付 | 4 天 |
| 第六阶段 | OpenDesign 式完成度 | 4 天 |
| 第七阶段 | 最终验收与比赛交付 | 2 天 |
| 集成缓冲 | 跨服务联调、回归和问题修正 | 5 天 |
| **合计** | **形成完整产品版** | **30 个工作日** |

周期按单主线开发估算。`25` 天是纯任务工时，必须保留 `5` 天跨服务集成缓冲。可以并行的工作仅限文件范围明确、没有共享状态的任务；数据主链、契约和数据库迁移必须串行完成。

## 六、阶段门槛

每个阶段只有满足以下条件才能进入下一阶段：

- 该阶段新增测试全部通过。
- 受影响包的类型检查通过。
- 不依赖预制中间产物证明成功。
- 不新增没有问题记录的 stub、占位页面或静默跳过逻辑。
- 失败场景有明确错误码和可定位日志。
- 文档描述与当前代码一致。
- 完成一次“Review 查 Bug → 第一性原理复核”，确认没有更简单的实现。

## 七、关键设计决定

### 7.1 不再让前端传可信配置

前端只传引用：项目 ID、岗位 ID、数据集 ID 和用户诉求。Profile、Role、Metrics 必须由 daemon 从受控存储加载。

### 7.2 不建立第二套数字来源

Dashboard Manifest、HTML、PDF、PPTX 和飞书卡片都引用同一 FactSet。任何导出过程不得重新计算业务指标。

Runner 请求不接收任意绝对路径。daemon 只传受控执行引用，Runner 根据固定数据根目录解析并再次验证边界。

### 7.3 不把整页 HTML 当成唯一产品模型

HTML 是呈现层，Dashboard Manifest 是产品层。没有 Manifest，就无法稳定实现筛选、版本、局部修改和发布。

每次 Query、Fact、Narrative 或 Maker 修正必须生成新的不可变 ArtifactVersion，禁止覆盖旧版本后丢失审计链。

### 7.4 多 Agent 按“问题归属”协作

- SQL 或口径问题回到 Query 或 Metrics 审批。
- 数据质量问题回到 Dataset 或 Fact 执行。
- 叙事问题回到 Narrative。
- 视觉问题回到 Maker Patch。
- 权限问题直接阻断，不允许模型自行修正权限规则。

### 7.5 禁止关键路径降级

- Runner 不可用：运行失败。
- HMAC 配置不一致：启动失败。
- Metrics 缺失：运行失败。
- Gate 配置读取失败：发布失败。
- 截图失败：视觉评审失败，不得假装通过。
- 审计写入失败：受监管环境下运行失败。

## 八、主要风险

| 风险 | 影响 | 处理原则 |
| --- | --- | --- |
| 当前工作区有大量未提交修改 | 计划文件行号和模块状态可能变化 | 开发前重新生成基线清单，不覆盖他人修改 |
| Profile、Metrics、Dataset 契约不统一 | 主链不断出现临时转换 | 第二阶段先统一契约，再接编排 |
| 模型输出不稳定 | 重试次数和成本失控 | 使用结构化契约、固定预算和问题路由 |
| HTML 产物过度自由 | 难以筛选、版本化和编辑 | 增加 Dashboard Manifest，不限制视觉自由 |
| 飞书真实凭据不可用 | 无法完成真实交付验收 | 飞书阶段开始前确认测试应用和回调域名 |
| Windows 中文路径与测试缓存 | CI 与本机结果不一致 | 固定工作区临时目录，不使用系统共享 pytest 目录 |
| 真实模型测试受外部端点影响 | 默认 CI 不稳定或产生费用 | 默认 CI 验证确定性数据链；发布验收单独运行真实模型全流程，两者都必须通过但不混为一个命令 |

## 九、最终验收指标

### 数据与事实

- 关键业务数字 FactSet 绑定率：`100%`。
- 未声明指标引用：`0`。
- 未授权字段泄漏：`0`。
- 无来源数字：`0`。

### 主链稳定性

- 真实文件端到端成功率：连续 `20` 次运行不出现流程性失败。
- 服务重启恢复成功率：`100%`。
- 重复请求去重成功率：`100%`。
- 取消后新增外部副作用：`0`。

### 看板质量

- 桌面端和移动端无溢出、遮挡和重叠。
- 发布链接、导出文件和飞书卡片使用同一版本产物。
- 任一关键事实可在三步内查看来源。

### 工程质量

- 全仓类型检查通过。
- 全仓测试通过。
- 真实端到端测试通过。
- 默认 CI 的确定性数据链测试通过，发布前真实模型全流程验收通过。
- 安全负例测试通过。
- 文档和运行命令可复现。

## 十、下一步

本计划确认后，严格按照 `Bifrost_后续开发方案_V2.md` 从“稳定工程基线”开始实施。由于当前工作区已经出现配置包、Run 请求契约、数据集注册器和运行上下文等新增文件，开始每个 Task 前先判断是“审查并补齐”还是“新建”，禁止照计划机械覆盖。第一轮开发不新增产品功能，只修复类型检查、测试污染、错误吞噬和配置不一致，为真实数据主链提供可靠基线。
