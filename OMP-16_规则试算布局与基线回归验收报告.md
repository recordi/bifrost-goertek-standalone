# OMP-16 规则试算布局与基线回归验收报告

## 变更范围

本次仅调整“管理配置”页面，新增“规则试算与影响预览”卡片，并补强公式输入框的可读性。看板中心、六角色视图、多产线对比、事件中心、数据治理、AI 助手、Overview/Event 载荷和 Skill 未修改。

## 交互能力

- 读取 `/api/rule-defaults` 展示当前规则版本、指标公式和目标/预警/严重阈值；
- 编辑候选公式、阈值和试算样本；
- 点击“试算影响”调用只读 `/api/rule-simulate`，返回基线与候选结果对比；
- 仅生成审批草稿，不发布规则、不写回历史事件、不覆盖原始数据；
- 长公式使用可换行文本框，避免管理人员无法看清或编辑公式。

## 验收结果

| 项目 | 结果 |
|---|---|
| 配置页卡片可见 | PASS |
| 规则默认值接口 | PASS（200） |
| 试算接口 | PASS（200） |
| 试算结果显示 | PASS（候选规则未改变当前样本结果） |
| 长公式输入可读 | PASS（双行文本框，宽度随卡片布局） |
| 规则引擎回归 | PASS（5/5） |
| AI 合同 UI 回归 | PASS（8 指标、3 风险、2 动作、证据折叠） |

## 基线保护

本次没有修改正式规则、历史事件、原始数据、载荷 JSON、角色投影或 Skill。当前运行中的 4173 旧进程需要重启后才能加载新增的两个规则接口。

